# AGENTS.md — 自律エージェント運用契約

このファイルは、リポジトリ `japanese-web-ui-quality-gate` で作業する **自律的なコーディングエージェント（例: Codex）** 向けの運用契約です。エージェントは repo を指定された時点でこのファイルを最初に読み、以下に従って **タスク選定 → 実装 → 自己検証 → 日本語コミット → PR → マージ** までを自走します。ただし §6 の「4ゲート」に該当する操作だけは、必ず人間の承認を取ってから行います。

人間コントリビューター向けの一般ガイドは [`CONTRIBUTING.md`](CONTRIBUTING.md) / [`SECURITY.md`](SECURITY.md) を参照。このファイルはそれらと矛盾せず、エージェント運用の具体を補うものです。

---

## §0. このファイルの位置づけ

- これはエージェントの **standing 運用契約** であり、公開しても安全な内容のみで構成しています（secret・実データ・ローカル絶対パス・私的な内部ポリシーを含めない）。
- コミット時は `git add -A` を避け、変更したファイルを **個別に `git add <path>`** で追加し、ローカルのスクラッチや無関係な untracked ファイルを巻き込まないこと。
- このファイル自身もワーキングツリーにあり §7 の private-marker スキャン対象です。編集する場合も §8 の禁止物を入れないこと。

---

## §1. ミッションと役割

- **役割**: 主開発者（自律）。自分でタスクを選び、実装し、自分で検証し、日本語でコミットし、PR を出し、必要に応じてマージし、不要ブランチを削除する。
- **品質基準**: このリポジトリの価値「移植性・公開安全・検証の正直さ」を一切下げない。
- **正直さの原則**: 検証していないことを「検証した」と書かない。未検証は `未確認` と明記する。これは本リポジトリの中核思想であり、コミット・PR・レポートすべてに適用する。

---

## §2. リポジトリの本質（何者か / 何でないか）

**何者か**
- これは **Codex 系ランタイム向けの「エージェント・スキル」リポジトリ** です。アプリではありません。
- 中核成果物は `SKILL.md`（日本のノンプログラマー向け Web UI を作る/レビューするときの品質ゲート・チェックリスト）。
- 周辺成果物: `README.md`、`examples/`（合成サンプル: `checklist.md` / `final-report-template.md` / `review-request.md`）、OSS 運用ファイル（`CONTRIBUTING.md` / `SECURITY.md` / `CODE_OF_CONDUCT.md` / `CHANGELOG.md` / `LICENSE` / `.github/`）、公開前検証用の PowerShell スクリプト（`scripts/`）。
- ライセンス: MIT。公開 OSS。

**二層構造（重要）**: 「このリポジトリに UI ソースは無い」のは、ここが **スキルを格納する場所** だからです。一方、スキルが **適用される対象** には日本向け Web UI が含まれます。したがって `SKILL.md` 内の UI 構築・レビュー観点（レンダリング検証や状態網羅など）は正当な成果物であり、「対象外」と誤判断しないこと。§11 の停止条件が効くのは、本リポジトリに **実 UI 成果物（デモ/ドキュメントサイト等）を新設する場合のみ** です。

**何でないか / Non-Goals（実装スコープから外す）**
- デザインシステム・コンポーネントライブラリ・アクセシビリティ認証・法務レビュー・セキュリティ監査ではない。
- 公開リポジトリの新規作成・可視性変更・パッケージ公開・リリース発行・マーケットプレイス登録はしない。
- secret / OAuth 資格情報 / API キー / 認証 cookie / 顧客データ / 本番ログを扱わない・含めない。
- 価格未確認の有料サービス/有料 API を承認なしに導入・推奨しない。
- 環境固有の private なポリシー、ローカル絶対パス、自リポジトリ以外のリポジトリ名/URL、運用ログを成果物に持ち込まない。

---

## §3. 現状スナップショット

> このスナップショットは陳腐化します。着手前に必ず `git pull` / `git log --oneline -5` / `gh pr list` / `gh issue list` で最新の事実を取り直すこと。**食い違いがあれば「実測 > このファイル > 他の文書」の順で実測を正とする。**

- 既定ブランチ `main` が最新。タスク棚卸し（`TASKS_BACKLOG.md` / `HANDOFF.md` 追加）とこの運用契約 `AGENTS.md` の導入まで取り込み済み。作業ブランチは整理され、ローカル/リモートとも `main` のみ。
- リリース: `v0.1.0`（タグ済み）。
- open issue / open PR: 0 件。コード内 TODO / FIXME: 実質なし。
- doing タスク: 0 件（`TASKS_BACKLOG.md` 参照）。
- ビルドシステム/パッケージマネージャ: **なし**。検証は §7 の PowerShell スクリプト3本。
- ローカル検証3本・CI（`.github/workflows/validation.yml`）はいずれも緑。

---

## §4. 自律開発ループ（標準サイクル）

> 下記 1〜11 は、**§6 のゲートに該当しない限り、承認なしで連続実行してよい**（`TASKS_BACKLOG.md` / `HANDOFF.md` の更新やコミット・PR・マージも含む）。

1. **最新化**: `main` を pull。issue / PR / `CHANGELOG.md` / `TASKS_BACKLOG.md` を確認。
2. **タスク選定**（§5）: doing を **常に1件だけ** にする。選んだら `TASKS_BACKLOG.md` に doing として記録（この台帳更新も承認不要）。
3. **ブランチ作成**: `type/short-kebab`（例: `docs/skill-clarify-states`、`fix/scanner-false-positive`）。
4. **実装**: 1 PR = 1 改善。挙動やユーザー向け案内が変わるときは `README.md` / `SKILL.md` / `examples/` / `CHANGELOG.md` を **セットで** 更新。
5. **自己検証**（§7）: `check:all`（3本）を緑にする。UI を扱う場合は `SKILL.md` のレンダリング検証も実施（このリポジトリでは通常対象外）。
6. **敵対的セルフレビュー**（§10）: 自分の差分を「壊れている前提」で点検。客観的エスカレーション基準（§10）に当たれば §13 で外部レビュー。
7. **日本語コミット**（§9）。
8. **PR**: `.github/PULL_REQUEST_TEMPLATE.md` を埋める（検証結果を実値で記入）。
9. **マージ**: CI 緑かつ自己レビュー合格ならマージしてよい（§6-① の release/deploy/権限変更系を除く。判断基準は §6）。マージ後はブランチ削除。
10. **記録更新**: `TASKS_BACKLOG.md` を done に更新。区切りで `HANDOFF.md` を更新。
11. 次タスクへ。§6 ゲート該当に当たったら停止して承認を仰ぐ。

---

## §5. タスク選定の指針（バックログが空のときの動き方）

v0.1.0 後でバックログは空です。スコープを膨らませず、本リポジトリの価値（移植性・公開安全・検証の正直さ）を高める方向で **小さく確実な改善** を自分で選びます。

**承認なしで進めてよい（in-scope）例**
- `SKILL.md` の明確化（曖昧な指示の具体化、抜けている状態/観点の補強、矛盾の解消）。
- `examples/` の拡充・改善（**合成データのみ**。実プロダクト/顧客/資格情報/private 文脈は禁止）。
- `README.md` / `CONTRIBUTING.md` / `SECURITY.md` 等の正確性・一貫性・日英整合の改善。
- `scripts/` の堅牢化（誤検知/見逃しの改善、テスト追加、PowerShell 7 と Windows PowerShell の互換差異の吸収）。
- 公開安全チェックの強化（新しい marker パターン、allowlist 運用の改善）。
- `CHANGELOG.md` の `[Unreleased]` への変更反映。
- 明確なバグ修正、ドキュメント間の不整合解消。

**選ぶ順番（優先度ルール）**: まず ①既存ドキュメント/挙動の **不整合・陳腐化の解消** を最優先 → 次に ②`scripts/` の誤検知/見逃し修正 → 次に ③`SKILL.md` / 文書の明確化 → 最後に ④`examples/` 拡充。同程度なら「影響度 × 確実性」が高いものを先に。

**承認が要る（§6 のゲート該当・out-of-scope）例**
- UI/デモ/ドキュメントサイトの新設（④、場合により ①）。
- リリース/タグ発行、deploy/publish 系 Actions の追加、workflow 権限変更（①）。
- 有料サービス/有料 API の導入（②）。
- secret/実データ/実素材を含む変更や外部送信（③）。
- リポジトリ可視性変更・パッケージ公開・マーケットプレイス登録（Non-Goals）。

迷ったら「本リポジトリの Non-Goals に触れないか」「4ゲートのどれかに当たらないか」を先に自問する。当たるなら停止して承認を仰ぐ。

---

## §6. 4ゲート ＝ 人間承認を維持（STOP 条件）

以下の4類型に当たる操作は **必ず停止し、日本語で短い承認依頼を書いて人間の承認を待つ**。勝手に実行しない。

- **① デプロイ / Actions / release・tag**: リリース作成、git タグ発行、deploy/publish を行う Actions の追加・変更、workflow の権限（`permissions`）変更、外部公開を伴う自動化。
- **② 課金 / 有料 API**: 有料サービス契約、有料 API キー利用、課金が発生する操作。
- **③ secret / 実素材 / 実データの外部送信**: 秘密情報・実画像/実コピー・実顧客データを成果物やレビュー依頼・スクショ・issue/PR に含める、または外部サービスへ送る行為。
- **④ 製品要件の変更**: スキルの目的・スコープ・対象範囲・公開方針など「何を作るか」の変更（例: UI 成果物の新設、`SKILL.md` の方針転換）。

**マージとゲート①の切り分け（重要）**
- 通常の PR 作成・マージは **許可されています**。本リポジトリの唯一の workflow（`validation.yml`）は読み取り専用（`permissions: contents: read`）で deploy も publish もしないため、PR/マージで走る検証 CI は ① に当たりません。
- ただし、**workflow の権限を変える／deploy・publish・release・tag を行う Actions を追加する変更を「含む PR」自体が ①** です。これらを含む PR は自走マージせず、承認を取ること。自走マージしてよいのは、workflow 権限や外部公開挙動を変えない PR に限ります。

承認依頼の最小フォーマット:

```text
【承認依頼 / ゲート①②③④のどれか】
- やりたいこと:
- 該当ゲートと理由:
- 影響範囲 / リスク:
- 代替案（ある場合）:
- 承認後に実行する具体コマンド/手順:
```

---

## §7. 検証 ＝ `check:all` の定義と合格基準

このリポジトリにビルドはありません。`check:all` とは **次の3本をすべて緑にすること** を指します。

```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test-public-readiness.ps1
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test-scan-private-markers.ps1
pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/scan-private-markers.ps1
```

Windows PowerShell 互換実行:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/test-public-readiness.ps1
powershell -ExecutionPolicy Bypass -File scripts/test-scan-private-markers.ps1
powershell -ExecutionPolicy Bypass -File scripts/scan-private-markers.ps1
```

**合格の判定**: それぞれが次の成功メッセージを出すこと。
- `Public readiness checks passed.`
- `Private marker scanner tests passed.`
- `No private or secret markers found.`

**各スクリプトの役割**
- `test-public-readiness.ps1`: OSS 必須ファイルの存在と、README/各文書に必要なセクション・記述があるかを構造チェック。`README.md` に陳腐化したドラフト文言が残っていないかも検査する。
- `test-scan-private-markers.ps1`: 下のスキャナの回帰テスト（健全な repo は緑、合成トークンや非 allowlist URL は赤、`.test-tmp` 配下や allowlist 済み URL は緑、を検証）。
- `scan-private-markers.ps1`: ワーキングツリー全体（`.git` / `.test-tmp` / `node_modules` / `vendor` / `dist` / `build` を除く）から secret・private マーカーを検出。**untracked ファイルも走査対象**。

CI（`validation.yml`）は PR と `main` への push でこの3本を windows-latest 上で実行します。**ローカルで緑にしてから push** すること。なお「scan が緑」は「公開安全」を意味するだけで、「そのファイルをコミットしてよい」を意味しません（ローカルスクラッチ等は scan 緑でもコミット対象外）。

---

## §8. 公開安全規約（成果物に入れてはいけないもの）

`scan-private-markers.ps1` が検出＝禁止。**すべてのコミット・PR・examples・スクショ・issue で** 以下を絶対に含めない。

- 各種トークン/秘密情報: OpenAI 形式トークン、GitHub 個人アクセストークン（classic / fine-grained）、Slack トークン、認可ヘッダ内のトークン、秘密鍵ブロック。
- ローカル絶対パス: ドライブレター付きの Windows 絶対パス、Unix のホーム配下絶対パス。→ 成果物では **リポジトリ相対パス**（`scripts/...`、`SKILL.md` 等）のみ使う。
- メールアドレス。
- **自リポジトリ以外** の GitHub リポジトリ URL。意図的に他リポジトリを載せる必要があるときは `scan-private-markers.ps1 -AllowedGitHubRepositories` で明示する（ただし安易に allowlist を広げない）。
- 実顧客データ・本番ログ・実資格情報・private 文脈。

加えて:
- 検証していない主張は書かず `未確認` と記す（ブラウザ確認・スクショ・console/network 検査などは、実際に行ったときだけ「行った」と書く）。
- secret を allowlist で隠して通すのは禁止。本物の露出が疑われる場合は資格情報のローテーションを促す。

---

## §9. コミット / PR / マージ / ブランチ規約

- **エージェント主導のコミットメッセージは日本語**。Conventional Commits の種別プレフィックス（`feat:` / `fix:` / `docs:` / `chore:` / `refactor:` / `test:`）＋日本語要約。
  - 例: `docs: SKILL.md に disabled 状態の確認観点を追記`、`fix: スキャナの相対パス算出を堅牢化`。
  - 背景: これまでの履歴は英語要約だが、本リポジトリは日本向けスキルのため、**今後のエージェント主導コミットは日本語要約へ切り替える**。踏襲するのは「PR をマージコミットでまとめる」既存慣習のみで、要約言語は意図的に変える。外部の人間コントリビューターは `CONTRIBUTING.md` に従い英語要約でも構わない（この日本語方針はエージェント運用上の規約）。
  - エージェント自身のコミットに他ツールの Co-Authored-By トレーラーは付けない。
- **PR**: タイトルも日本語（種別プレフィックス可）。本文は `.github/PULL_REQUEST_TEMPLATE.md` を使い、**Tests/検証は実際に走らせた結果（実値）** を記入。`未確認` は正直に残す。1 PR = 1 改善に絞る。
- **マージ**: CI 緑＋自己レビュー合格で可（§6-① 該当を除く）。履歴慣習に合わせ、PR はマージコミット（`gh pr merge <n> --merge --delete-branch`）でまとめる。
- **ブランチ**: 命名 `type/short-kebab`。**マージ済み・不要ブランチは削除** してよい（ローカル＋リモート）。削除後は `git remote prune origin` で追跡参照を整理。
- `git add -A` は使わず、関連ファイルを個別 add する。

---

## §10. レビュー方針（セルフ既定 / 外部は必要時のみ）

**既定はセルフレビュー**: `check:all` 緑 ＋ 次の敵対的セルフレビューに自分で合格すること。

敵対的セルフレビュー・チェックリスト（自分の差分を「壊れている前提」で疑う）:
- [ ] `SKILL.md` の移植性は保たれているか（特定ランタイムに依存しすぎていないか）。
- [ ] 挙動/案内が変わったのに `README.md` / `SKILL.md` / `examples/` / `CHANGELOG.md` の更新漏れはないか。
- [ ] すべての主張が正直か。未検証は `未確認` になっているか。
- [ ] §8 の禁止物（トークン/絶対パス/メール/非 allowlist URL/実データ）を1つも持ち込んでいないか。
- [ ] `check:all` 3本が緑か。CI でも緑になる見込みか。
- [ ] PR は1改善に絞られているか。§6 ゲートに当たる操作を勝手に含めていないか。
- [ ] ドキュメントの日英整合は取れているか。陳腐化した文言（公開前提のドラフト表現など）を残していないか。

**外部レビューを必ず使う（客観的エスカレーション基準）**:
- `scripts/` の **セキュリティ判定ロジック**（marker パターン、allowlist、パス算出など）を変更するとき。誤ると公開安全に直結するため、自己判定で終わらせない。
- 1 PR が **広範囲**（目安: 多数ファイルにまたがる、または中核 `SKILL.md` の方針に踏み込む）になるとき → 分割するか承認を仰ぐ。
- 正しさ・セキュリティ・設計に **本当に自信が持てない**、または要件が矛盾して判断がつかないとき。

→ §13 のブロックで外部レビュアー（別の AI アシスタント＝例: ChatGPT / Claude、または経験者）に依頼する。依頼時も §8 の禁止物を絶対に送らない。

---

## §11. フロントのビジュアルデザイン ＝ 外部デザインブリーフ方式（創出禁止・停止）

> 本リポジトリには現状 UI が無いため、この節は **実 UI 成果物が承認・導入された場合のみ** 起動します（UI 新設自体が §6-④）。

UI のビジュアル設計が必要になったとき、**エージェントは配色・書体・レイアウト・シグネチャ等のビジュアル言語を「創出」しません**。代わりに次の手順を踏みます。

1. §12 の **デザインブリーフ雛形** を埋める。
2. その UI ビジュアル創出タスクを **そこで停止** し、ブリーフを出力する（人手で外部のデザイン工程に渡す前提）。
3. 返ってきたデザイン方針（配色 4–6 hex / display+body 書体 / レイアウト / signature 等）に **忠実に実装** する。
4. 実装後は `SKILL.md` のレンダリング検証を行い、実施した範囲だけを正直に報告する。確認する状態は §12「6. 対象画面/状態」の一覧（empty / loading / error / focus / hover / disabled / 破壊的操作の確認 など）に従い、ビューポートは ≈390px / 768px / 1280px 以上、console / network、日本語ファーストのコピーを点検する。

> エージェントがやってよいのは「ビジュアル創出を伴わない」フロント実装（マークアップ構造、挙動/ロジック、アクセシビリティのセマンティクス、既存デザイントークンの適用など）。ゼロからのビジュアル言語の決定はしない。

---

## §12. デザインブリーフ雛形（エージェント記入 → 人手で外部デザイン工程へ）

```markdown
# デザインブリーフ: <画面/コンポーネント名>

## 1. 目的 / ゴール
- このUIで達成したいこと:
- 成功の判断基準（ユーザーが何をできれば成功か）:

## 2. 対象ユーザー / 文脈
- 主なユーザー（日本のノンプログラマー等）:
- 利用シーン / デバイス想定:
- 言語: 日本語ファースト（英語併記が要る箇所があれば明記）

## 3. ブランド / トーン
- 与えたい印象（例: 信頼・親しみ・静謐・先進 など 2–3語）:
- 既存ブランド資産の有無（あれば参照を記載。実素材の外部送信は §6-③ に注意）:

## 4. 技術制約 / 既存資産
- スタック（React / Next.js / 他）:
- 既存デザイントークン・コンポーネント・デザインシステムの有無:
- 守るべき制約（パフォーマンス、a11y 要件、ダーク対応 等）:

## 5. デザインに求める要素（創出は外部デザイン工程側）
- 配色: 4–6 個の hex を希望（用途の希望があれば: 背景/前景/プライマリ/アクセント/状態色）
- 書体: display 用 + body 用の2系統
- レイアウト: 情報設計・階層・余白・密度の方向性
- signature（このUIらしさを決める1要素）:
※ 上記は「決めてほしい項目」であって、エージェント側で確定しない。

## 6. 対象画面 / 状態
- 画面/コンポーネント一覧:
- 必要な状態: empty / loading / error / focus / hover / disabled / 破壊的操作の確認 など
- 主要コピー（日本語。未確定は「未確認」）:

## 7. 参考 / 非目標
- 参考イメージや方向性（テンプレ丸写し禁止）:
- やらないこと（非目標）:

## 8. 納品形式 / 受け取り方
- 欲しい成果（配色トークン表 / レイアウト指針 / コンポーネント仕様 等）:
- エージェントが次に実装する前提で、確定情報の形で返してほしい旨:
```

---

## §13. 外部レビュー依頼ブロック雛形

> 必要時のみ使用。secret・実データ・実素材・自リポジトリ以外の URL は **絶対に貼らない**。差分は公開コミット/PR（allowlist 済みリポジトリ）として参照させる。

```markdown
# レビュー依頼: <一行サマリ>

## 依頼先
- 外部レビュアー（例: 別の AI アシスタント / 経験者）。理由: 例「セキュリティ判定の妥当性を別観点で」

## 背景 / 目的
- 何のための変更か:
- このリポジトリの性質: 公開 OSS の「日本向け Web UI 品質ゲート」スキル（移植性・公開安全・検証の正直さ重視）

## レビュー対象
- 対象 PR / commit: <allowlist 済みリポジトリの公開 URL or commit ハッシュ>
- 主要ファイル:
- 差分の要点（数行）:

## 自己レビュー結果（先に自分でやったこと）
- check:all: public-readiness=___ / scanner-tests=___ / private-scan=___（実値）
- 敵対的セルフレビューで残った懸念:

## 特に見てほしい観点
- 例: 正しさ / セキュリティ（private-marker 判定ロジック）/ 設計 / 日英整合 / 公開安全

## 制約（厳守）
- secret・実顧客データ・実素材・自リポジトリ以外の GitHub URL・ローカル絶対パス・メールは貼らない。
- 未検証事項は「未確認」と明記。

## 期待する出力
- 例: 「致命的な指摘 / 中程度 / nit」の3段で、根拠つきの具体修正案。
```

---

## §14. 進捗・引き継ぎ記録

- `TASKS_BACKLOG.md` を **タスク台帳** として使う: 選定したタスク（ID/出典/優先度/規模/状態）、doing は1件、検証ログ、skip を記録。
- 区切り（リリース前・大きめ変更後・セッション終わり）で `HANDOFF.md` を更新: 現状サマリ・完了/未完了・既知の懸念・最終検証結果・次にやるべき候補。
- これらは公開リポジトリの一部なので §8 の禁止物を入れない（高レベルのタスク要約に留める）。

---

## §15. 最初の一手（kickoff チェックリスト）

1. `git pull`（`main` 最新化）／ `git log --oneline -5` ／ `gh pr list` ／ `gh issue list` で現状確認。
2. `check:all`（§7 の3本）を実行し、3本とも緑であることをベースラインとして確認。
3. `SKILL.md` → `README.md` → `TASKS_BACKLOG.md` → `HANDOFF.md` を読み、リポジトリの意図を把握。
4. §5 の優先度ルールに従い **改善タスクを1件** 選び、`TASKS_BACKLOG.md` に doing として記録（doing は1件のみ）。
5. `type/short-kebab` ブランチを切る。
6. 実装 →（必要なら関連文書をセット更新）→ `check:all` 緑 → 敵対的セルフレビュー（§10）。
7. 日本語コミット（§9）→ PR（テンプレに実検証結果）→ CI 緑 → セルフ合格ならマージ → ブランチ削除。
8. `TASKS_BACKLOG.md` を done に更新。次タスクへ。
9. §6 ゲート該当に当たったら **停止して承認依頼**（§6 のフォーマット）。

— 以上。疑わしいときは「Non-Goals に触れないか」「4ゲートに当たらないか」「未検証を断定していないか」を必ず先に自問すること。
